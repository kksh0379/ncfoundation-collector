# 📅 변경 이력 (일자별)

> 관리자용 배포/변경 기록입니다. 최신이 위.
> **버전 규칙**: 정식 출시(1.0) 전이라 `v0.NN` 으로 표기하고, 뒤에 `build YYMMDD`(빌드 날짜)를 붙여요.
> 예: `v0.39 · build 260924`. (아래 과거 항목의 vNN 은 v0.NN 과 같은 번호예요: v38 = v0.38)

## 2026-09-24
- **v0.49 · build 260924** — **자동 백필 기본 OFF**: 부팅 직후 2년치 대량 수집이 무료 512MB에서 OOM→프로세스 크래시 루프→사이트 접속 불가(ERR_CONNECTION_TIMED_OUT)를 일으켜서, 부팅 때 수집을 하지 않도록 변경. 데이터는 관리자 수집/🗑 초기화·cron으로 채움. 켜려면 `AUTO_BACKFILL=1`.
- **v0.48 · build 260924** — **배포 안정화(중요)**: 부팅(import) 때 Neon에 동기 접속하던 걸 제거. DB 초기화는 첫 요청 때 지연 실행(lazy), 자동 백필은 백그라운드 스레드로. Neon이 자고 있어도(cold start) gunicorn 워커가 죽지 않아 Render 배포가 실패하지 않는다. (그동안 새 버전이 배포 안 되고 v0.41에 멈춰 있던 유력 원인)
- **v0.47 · build 260924** — 수집 속도 개선: 수집 전 '기존 데이터 확인'(전체 URL 로드) 단계 제거 + 저장 시 신규 건수를 전체 URL 스캔 대신 COUNT 차이로 계산(큰 테이블·Neon cold start에서 훨씬 빠름). 중복은 저장 시 ON CONFLICT로 처리. **뉴스 수집 기간 선택 UI 제거**(항상 최근 2년). (DB 초기화는 이전부터 뉴스·게시판·소셜 전체에 동작)
- **v0.46 · build 260924** — 뉴스 카드 **이미지(썸네일) 롤백**(og:image 추출·보강·image_url 제거). 카드 요약 **4줄 유지**(한 줄 더). **DB 초기화 버튼을 헤더 상단으로 이동**(공통 동작이라 뉴스 탭에서 분리, '🗑 초기화').
- **v0.45 · build 260924** — 뉴스 카드에 **대표 이미지(썸네일)** 추가(og:image, 없으면 언론사 파비콘 → 글자 배지 폴백). 카드 **요약 3줄→4줄**로 확대. 수집 때마다 이미지 없는 최근 기사 og:image를 조금씩 **자동 보강**(`IMG_ENRICH_MAX`, 대량 백필은 스냅샷만이라 이미지가 비는 걸 이후 채움). 조회 상한 1000→3000(2년치가 화면에 다 보이도록). 뉴스에 `image_url` 컬럼 추가.
- **v0.44 · build 260924** — 관리자 **🗑 DB 비우기** 기능 추가(뉴스·게시판·소셜 전체 삭제 후 최근 2년치로 자동 재수집, `/api/admin/purge`). 증분 수집이라 안 지워지던 초기 실패 잔여 데이터를 통째로 정리 가능. 개발노트를 **개발자 1인칭 노트 문체**로 전면 재작성.
- **v0.43 · build 260924** — 수집 기간 5년 → **2년**으로 축소(백필 기본값 1825→730). 5년 백필이 RSS 구간을 너무 많이 돌아 크롤링이 길어지고 그 사이 Neon이 잠들었다 깨며 'couldn't get a connection'·메모리 초과를 유발하던 문제를 근본적으로 완화. 뉴스 필터 상한(RECENT_DAYS=730)과도 일치. 관리자 수집 기간 기본 선택도 '최근 2년'으로.
- **v0.42 · build 260924** — Neon cold start로 인한 'couldn't get a connection after 30s' 재발 완화: 연결 풀 대기 30→45초, 연결 획득 실패 시 자동 재시도(최대 3회). 5년 수집처럼 RSS 단계가 길어 DB가 잠들었다 깨는 경우에도 견디게.
- **v0.41 · build 260924** — 본사 노이즈 제외 강화: 야구(NC 다이노스)·NC백화점(뉴코어) 등 제외어 확장 + 모호한 NC/엔씨는 '게임회사 문맥어(게임/소프트/리니지 등)' 있을 때만 인정. 수집 시 기존에 쌓인 본사 노이즈도 자동 삭제(정리).
- **v0.40 · build 260924** — 대량 수집 폭주/멈춤/‘불러오기 실패’ 해결. 새 기사가 많으면(>150) 원문 해석(batchexecute) 생략하고 RSS 요약만 사용(구글 차단·지연 회피). 그룹화는 대량(>600)일 때 N×N 유사도 대신 제목 기준(O(N))으로 → 메모리 초과(OOM) 방지.
- **v0.39 · build 260924** — 버전 넘버링 리마스터(vNN → v0.NN + build 날짜). 저작권 김상화. 개발노트 상세 재작성 + 앱 내 노트 마크다운 렌더.
- **v38** — 푸터를 **항상 보이는 얇은 고정 바**로(하단 고정, 스크롤 무관). 저작권(© 2026 kksh0379)·버전·저장상태 표시. 본문 하단 여백/맨위로 버튼 위치 조정.
- **v37** — **푸터 추가**(앱명·설명·버전·저장상태·출처). 버전 표기를 헤더에서 **푸터로 이동**, 헤더는 제목+버튼만.
- **v36** — 관리자 비번을 **입력창 자동채움**용으로만 캐시(자동 로그인은 안 함). 로그인 창 열면 저장된 비번이 채워져 매번 타이핑 불필요.
- **v35** — 뉴스(기사) 탭 범례를 **관리자 전용**으로(게시판·소셜 범례는 공개 유지). 관리자 **비밀번호 로컬 저장→자동 로그인**(매번 입력 불필요, 로그아웃 시 저장 비번 삭제).
- **v34** — **구글 100건 제한 우회**: 수집 기간을 120일 구간으로 쪼개 `after:`/`before:` 날짜검색으로 각 구간 수집 → **과거(최대 5년)까지 깊게** 수집. UI: 필터 선택 시 **로딩 표시**, **맨 위로 플로팅 버튼**, 목록 **무한 스크롤**. 조회 상한 상향(뉴스 1000/게시판·소셜 500).
- **v33** — '수집 대기' 멈춤 방지(URL만 경량 로드+멈춘 작업 30분 후 자동 해제), 수집 기간 드롭다운을 **관리자 전용**(수동수집 세트)으로. 개발노트/패치내역(관리자 전용 📓 노트).
- **v32** — 정기 배치 수집 창을 2년→**최근 30일**로 축소(최초 5년 백필과 역할 분리). `BATCH_DAYS` 환경변수 추가.
- **v31** — **관리자 로그인 부활**(비밀번호 로그인, 세션): 로그인해야 상태확인·수집 버튼 노출·동작. 수집 기간 **5년 옵션** 추가. **빈 DB 자동 백필**(앱 시작 시 비어 있으면 5년치 자동 수집 → 배포하면 버튼 없이 기사 표시).
- **v30** — **Neon 연결 안정화**: `channel_binding` 제거 + 연결 풀 견고화(cold start/유휴 끊김 대응). "couldn't get a connection" 해결.
- (설정) **외부 Postgres(Neon) 연결**: `DATABASE_URL` 등록 → 데이터 영구 저장. **cron-job.org + `CRON_TOKEN`** 로 정기 자동 수집.

## 2026-09-23
- **v29** — 자동 수집(외부 크론) + 상태확인/수집 **관리자 전용화**(당시 키 방식, 이후 v31에서 로그인으로 교체). `/api/cron` GET/POST·비차단.
- **v28** — 저장소 종류 배지(☁ 영구 / ⚠ 임시) 표시로 DB 연결 확인 용이.
- **(DB)** — SQLite/Postgres 이중 지원(`DATABASE_URL` 있으면 Postgres). ON CONFLICT 배치 저장.
- **v27** — 뉴스 **수집 기간 선택**(1/3/6개월·1/2년, 기본 3개월).
- **v25~v26** — 뉴스 **재단/본사 카테고리** + 셀렉트, 본사=엔씨소프트+**자회사**(NC AI/QA/IDS·게임스튜디오), 야구 제외. 화면 꺼짐/백그라운드에도 수집 유지(서버 백그라운드 작업 + 폴링).
- **v24** — 본사 키워드 정제(NC/엔씨 단어 단위 매칭 + 야구 노이즈 제외).
- **v22~v23** — 100개 제한 제거(전량 수집), 속도 최적화(병렬 24·짧은 타임아웃), 증분 수집.
- **v19~v21** — 뉴스 증분 수집(저장키=구글 링크 고정) + 진행률 실시간 표시, 원문 URL 분리(source_url).
- **v16~v18** — 동일 기사 그룹화(아코디언), 최근 N년 필터, 진행상황 스트리밍.
- **v13~v15** — 관리자 로그인 제거(당시) + 모바일 셀렉트 UI 수정(커스텀 드롭다운), 상태확인에 "추출 글 수" 표시.
- **게시판 수정** — 프로젝토리 SSL 인증서 우회(verify 폴백) + 실제 선택자 적용, `/api/inspect` API 헌터/probe 추가(SPA 내부 API 탐색).
- **뉴스 본문** — 구글 뉴스 신형 URL 디코더(batchexecute)로 원문 복원, 실패 시 RSS 요약 폴백.

## 2026-06 (최초 구축)
- Flask 앱 초안: 탭 3개(뉴스·게시판·소셜), 구글 뉴스 RSS 수집, 게시판 크롤러, 소셜(유튜브 RSS), SQLite 저장, 중복 판단(TF-IDF), Render 배포 설정.
