# 📅 변경 이력 (일자별)

> 관리자용 배포/변경 기록입니다. 최신이 위.
> **버전 규칙**: 정식 출시(1.0) 전이라 `v0.NN` 으로 표기하고, 뒤에 `build YYMMDD`(빌드 날짜)를 붙여요.
> 예: `v0.39 · build 260924`. (아래 과거 항목의 vNN 은 v0.NN 과 같은 번호예요: v38 = v0.38)

## 2026-09-24
- **v0.89 · build 260924** — FAIR AI 인사이트는 목록 API를 특정 못 해(공지와 다른 방식·401) 당분간 건너뜀 처리. FAIR AI는 공지사항만 수집(정상).
- **v0.88 · build 260924** — FAIR AI 공지사항 수집(POST `ai-notice-page`, body {page,rowsPerPage,sortBy,sortType,keyword} → items[]{id,title,contents}). 제목·요약(contents)·본문이미지·상세링크. 인사이트는 엔드포인트 확인 중.
- **v0.87 · build 260924** — /api/peek에 POST 옵션(method=post&body=) 추가. FAIR AI 목록 API가 POST(ai-notice-page)인 것 확인 후 테스트용.
- **v0.86 · build 260924** — 전체 청크 검색 `/api/grepall?url=&q=` 추가(모든 JS 청크에서 검색어 주변 코드 반환). FAIR AI 인증 토큰/API 클라이언트 탐색용.
- **v0.85 · build 260924** — /api/peek에 ref= 옵션(Origin/Referer 헤더) 추가. Origin 검사형 API(FAIR AI 등) 인증 테스트용.
- **v0.84 · build 260924** — apihunt 보강: Nuxt/Vite modulepreload(`<link href>`)·import() 청크까지 모두 스캔(최대 25개), $fetch·/fair/api·notice/insight 패턴 추가. FAIR AI(Nuxt SPA) 목록 API 탐색용.
- **v0.83 · build 260924** — 프로젝토리 텍스트 글 "요약 없음" 해결: 목록 API 응답에 본문(contents)이 포함돼 있어 상세 호출 없이 바로 요약. (상세 news-view는 껍데기라 불필요) 제목 필드 title 대응.
- **v0.82 · build 260924** — 나의AAC 전면 보강: (1) 카드 썸네일을 상세 본문 이미지 우선으로(사이트 기본 로고 회피, 상대경로 해결), (2) `?page=N` 페이지네이션으로 과거 글(최초 2024.3)까지 수집(최대 15페이지). 게시판 목록 파싱을 페이지 단위로 리팩터, 이미 저장된 글은 상세 재요청 생략(증분).
- **v0.81 · build 260924** — 나의AAC 카드 썸네일(상세 og:image) 추가. 프로젝토리 텍스트 글의 "요약 없음" 해결: 상세 API(news-view?boardIdx=)에서 본문 받아 요약(증분, 이미 있는 건 건너뜀).
- **v0.80 · build 260924** — 나의AAC 링크 수정: 글 링크가 `javascript:goDetail(270)` 형태라 seq를 못 뽑아 목록으로 떨어지던 것 해결(goDetail·괄호 안 따옴표 패턴 추출). 이제 상세글(announcementDetail.do?seq=)로 정확히 이동.
- **v0.79 · build 260924** — 프로젝토리 게시판 **수집 완료**: 모바일 사이트의 axios API(`/news/notice-list` 등, params 페이지네이션)를 직접 호출해 공지·이야기·갤러리 수집. 제목·작성일·썸네일(fileUrl)·글링크(boardIdx). 상태 "완료".
- **v0.78 · build 260924** — 파일 내용 검색 진단 `/api/grep?url=&q=` 추가(JS에서 특정 함수/문자열 주변 코드 확인). 프로젝토리 _sendAxios 호출 방식 분석용.
- **v0.77 · build 260924** — /api/peek에 xhr=1 옵션 추가(X-Requested-With 헤더로 AJAX 응답 유도). 프로젝토리처럼 같은 URL이 XHR일 때 JSON을 주는 사이트 확인용.
- **v0.76 · build 260924** — apihunt 보강: 페이지 HTML의 인라인 스크립트까지 검색(라이브러리 파일 제외), axios/ajax/url 패턴 추가, script_hits(관련 줄 원문) 반환. 프로젝토리처럼 Vue+axios 인라인 호출 사이트 API 탐색용.
- **v0.75 · build 260924** — 소셜 카드 썸네일: 유튜브 영상 썸네일(i.ytimg.com/…/hqdefault.jpg, RSS media:thumbnail 우선), 인스타 게시물 썸네일 표시. social에 image_url 컬럼.
- **v0.74 · build 260924** — 게시판 카드에 **썸네일 이미지** 표시(대표 홈페이지 목록 API의 thumbnail 사용). 이미지만 있는 글은 요약 대신 이미지가 보이고 "요약 없음" 안 뜸. boards에 image_url 컬럼. 기존 요약은 보존하며 썸네일만 채우는 증분 처리.
- **v0.73 · build 260924** — 게시판 이미지만 있는 글의 "요약 없음"을 안내 문구("🖼 이미지로 구성된 게시물…")로 대체.
- **v0.72 · build 260924** — 대표 홈페이지 "요약 없음" 해결: 새 글은 상세 API에서 본문을 받아 요약 채움(증분 — 이미 요약된 글은 건너뛰고, 빈 글은 다음에 재시도해 자가복구).
- **v0.71 · build 260924** — 대표 홈페이지 게시판 **수집 완료**: 내부 API(`api.ncfoundation.or.kr/community/all`)를 직접 호출해 전체 목록(제목·작성일·글 링크 /community/{dtype}/{id}) 수집. 상태 "완료".
- **v0.70 · build 260924** — /api/peek에 본문 앞부분(body_head) 포함(JSON API 응답 구조 확인용). 대표 홈페이지 API(api.ncfoundation.or.kr) 연동 준비.
- **v0.69 · build 260924** — SPA API 탐색 `/api/apihunt?url=` 추가: JS 번들을 받아 그 안에 박힌 API 주소 후보를 추출(CRA SPA인 대표 홈페이지의 목록 API 찾기용).
- **v0.68 · build 260924** — 가벼운 구조 진단 `/api/peek?url=` 추가(단일 요청으로 링크·embedded JSON 요약). /api/inspect가 무거워 502 나던 문제 우회.
- **v0.67 · build 260924** — 대표 홈페이지 글 주소 패턴 반영(`/community/all/{번호}`). 목록 URL `/community/all`, 링크 선택자·상세 경로(detail_path) 맞춤.
- **v0.66 · build 260924** — 대표 홈페이지 게시판 수집 재시도: 대상 URL을 `ncfoundation.or.kr/community/`로 변경. 정적 목록이 없을 때 페이지에 박힌 JSON(__NEXT_DATA__/application-json/__NUXT__)에서 글을 뽑는 폴백 추가. 상태 "구현 중".
- **v0.65 · build 260924** — 카드 본문이 2줄뿐이던 원인 해결: 대량 수집은 구글 RSS 요약(제목+언론사 수준)만 저장돼 짧았음. 보강 단계에서 원문을 열 때 대표 이미지와 함께 **실제 요약(og:description/본문)** 도 채우도록 확장 → 수집 반복하면 본문이 3~4줄로 채워짐.
- **v0.64 · build 260924** — 접속자 로그 추가: 방문 시각·IP·기기(UA요약: 모바일/PC·OS·브라우저)·언어를 기록하고, 관리자 📋 로그 팝업에 "접속 로그" 탭 추가(수집 로그와 함께). 관리자 본인·봇/헬스체크는 제외. 카드 본문 6→4줄, **기본 보기=카드형**으로 변경.
- **v0.63 · build 260924** — 수집 실행 로그 추가: 관리자 헤더 **📋 로그**에서 각 수집이 언제 돌았고 성공/실패인지(신규·갱신 건수 포함) 최근 기록을 확인. run_log 테이블·`/api/runlog`.
- **v0.62 · build 260924** — 수집 기간 **5년**으로 확대(뉴스 RECENT_DAYS/백필 1825). 보기 방식 **리스트/카드 선택** 추가(기본 리스트=제목 한 줄 …, 선택 저장). 카드 제목 클릭 시 원문 이동. 개발노트를 일반 문체(외부인이 읽어도 자연스럽게)로 정비.
- **v0.61 · build 260924** — 앱 명칭/BI 리브랜딩: "엔씨문화재단 콜렉터" → **"NC 스코프"**. 조준경(렌즈) 형태의 SVG 로고 마크 + 워드마크를 헤더에 반영. BI 클릭 시 홈(/)으로 이동.
- **v0.60 · build 260924** — 이미지 없는 기사는 썸네일 숨김(파비콘·글자배지 제거). 동일 기사 그룹화 강화: 제목이 달라도 본문 유사도가 높으면 묶음. 대량에서도 메모리 안전하게 "같은 날짜" 블록 안에서 본문 유사도 비교(기존엔 대량이면 제목 같은 것만 묶었음).
- **v0.59 · build 260924** — 기사 카드 **썸네일 이미지** 추가(og:image, 없으면 언론사 파비콘→글자 배지 폴백, 상단 대형 배치). 수집 시 이미지 없는 최근 기사 og:image 자동 보강(`IMG_ENRICH_MAX`, 기본 200). 카드 본문 **4줄→6줄**. news에 image_url 컬럼.
- **v0.58 · build 260924** — 연결 방식을 풀(psycopg_pool)에서 **요청마다 직접 접속**으로 변경. 같은 문자열로 직접 접속은 되는데 풀만 "couldn't get a connection"으로 계속 실패하던 문제 해결(트래픽 적은 앱엔 직접 접속이 더 단순·확실). cold start 재시도 3회.
- **v0.57 · build 260924** — DB keep-alive 추가: 앱이 4분마다 DB에 가벼운 쿼리를 날려 Neon이 잠들지 않게 유지(Render 상시가동과 세트). 방문자가 잠든 DB를 안 만나 응답이 항상 빠름. `DB_KEEPALIVE_SEC`로 조절/해제.
- **v0.56 · build 260924** — 어떤 Postgres(특히 Supabase 트랜잭션 풀러)에도 안전하게 붙도록 prepared statement 자동생성 비활성화(prepare_threshold=None). DB 교체(예: Neon→Supabase) 대비.
- **v0.55 · build 260924** — 킵얼라이브용 초경량 `/healthz` 추가(DB 미접속). 외부 크론이 주기 호출하면 Render 무료 앱이 안 잠들어 방문자 cold start 감소.
- **v0.54 · build 260924** — 수집/초기화 시 DB를 실제로 깨우도록 수정. 직전 웹요청 실패의 쿨다운 때문에 수집이 DB를 안 깨우고 건너뛰어 실패하던 문제 해결(`_ensure_db(force=True)` — 쿨다운 무시하고 접속 대기해 Neon 깨움).
- **v0.53 · build 260924** — 🗑 초기화 오류(`The string did not match the expected pattern`) 수정. 원인: 서버가 에러 시 JSON이 아닌 응답을 줘서 화면이 못 읽음 + 테이블 미생성 상태에서 삭제 시도. → 수집/초기화 전에 DB(테이블) 보장, 오류도 항상 JSON으로 반환, 화면도 비정상 응답에 견고하게.
- **v0.52 · build 260924** — `/api/dbcheck` 진단을 로그인 없이 열 수 있게(비번 마스킹, 연결 성공/에러만 표시). Neon 연결 원인 파악 편의.
- **v0.51 · build 260924** — DB 연결 진단 엔드포인트 `/api/dbcheck`(관리자 전용) 추가. 풀/재시도 없이 직접 접속해 실제 에러 원문(인증 실패/호스트 불통/타임아웃 등)을 그대로 보여줘 원인 파악. 비번은 마스킹.
- **v0.50 · build 260924** — **DB가 죽어도 사이트는 뜨게(중요)**: 매 요청마다 45초 DB 연결을 시도해 스레드가 막히고 502가 나던 문제 해결. 요청을 막던 before_request 제거, DB 초기화는 실패 시 30초 쿨다운(요청 즉시 반환), 조회 API는 DB 불가 시 빈 목록 즉시 반환, 연결 대기 45→15초로 단축. 푸터에 'DB 연결 안 됨' 상태 표시. (메인 페이지는 DB 없이도 즉시 렌더)
- **v0.49 · build 260924** — **자동 백필 기본 OFF**: 부팅 직후 2년치 대량 수집이 무료 512MB에서 OOM→프로세스 크래시 루프→사이트 접속 불가(ERR_CONNECTION_TIMED_OUT)를 일으켜서, 부팅 때 수집을 하지 않도록 변경. 데이터는 관리자 수집/🗑 초기화·cron으로 채움. 켜려면 `AUTO_BACKFILL=1`.
- **v0.48 · build 260924** — **배포 안정화(중요)**: 부팅(import) 때 Neon에 동기 접속하던 걸 제거. DB 초기화는 첫 요청 때 지연 실행(lazy), 자동 백필은 백그라운드 스레드로. Neon이 자고 있어도(cold start) gunicorn 워커가 죽지 않아 Render 배포가 실패하지 않는다. (그동안 새 버전이 배포 안 되고 v0.41에 멈춰 있던 유력 원인)
- **v0.47 · build 260924** — 수집 속도 개선: 수집 전 '기존 데이터 확인'(전체 URL 로드) 단계 제거 + 저장 시 신규 건수를 전체 URL 스캔 대신 COUNT 차이로 계산(큰 테이블·Neon cold start에서 훨씬 빠름). 중복은 저장 시 ON CONFLICT로 처리. **뉴스 수집 기간 선택 UI 제거**(항상 최근 2년). (DB 초기화는 이전부터 뉴스·게시판·소셜 전체에 동작)
- **v0.46 · build 260924** — 뉴스 카드 **이미지(썸네일) 롤백**(og:image 추출·보강·image_url 제거). 카드 요약 **4줄 유지**(한 줄 더). **DB 초기화 버튼을 헤더 상단으로 이동**(공통 동작이라 뉴스 탭에서 분리, '🗑 초기화').
- **v0.45 · build 260924** — 뉴스 카드에 **대표 이미지(썸네일)** 추가(og:image, 없으면 언론사 파비콘 → 글자 배지 폴백). 카드 **요약 3줄→4줄**로 확대. 수집 때마다 이미지 없는 최근 기사 og:image를 조금씩 **자동 보강**(`IMG_ENRICH_MAX`, 대량 백필은 스냅샷만이라 이미지가 비는 걸 이후 채움). 조회 상한 1000→3000(2년치가 화면에 다 보이도록). 뉴스에 `image_url` 컬럼 추가.
- **v0.44 · build 260924** — 관리자 **🗑 DB 비우기** 기능 추가(뉴스·게시판·소셜 전체 삭제 후 최근 2년치로 자동 재수집, `/api/admin/purge`). 증분 수집이라 안 지워지던 초기 실패 잔여 데이터를 통째로 정리 가능. 개발노트를 **개발자 1인칭 노트 문체**로 전면 재작성.
- **v0.43 · build 260924** — 수집 기간 5년 → **2년**으로 축소(백필 기본값 1825→730). 5년 백필이 RSS 구간을 너무 많이 돌아 크롤링이 길어지고 그 사이 Neon이 잠들었다 깨며 'couldn't get a connection'·메모리 초과를 유발하던 문제를 근본적으로 완화. 뉴스 필터 상한(RECENT_DAYS=730)과도 일치. 관리자 수집 기간 기본 선택도 '최근 2년'으로.
- **v0.42 · build 260924** — Neon cold start로 인한 'couldn't get a connection after 30s' 재발 완화: 연결 풀 대기 30→45초, 연결 획득 실패 시 자동 재시도(최대 3회). 5년 수집처럼 RSS 단계가 길어 DB가 잠들었다 깨는 경우에도 견디게.
- **v0.41 · build 260924** — 본사 노이즈 제외 강화: 야구(NC 다이노스)·NC백화점(뉴코어) 등 제외어 확장 + 모호한 NC/엔씨는 '게임회사 문맥어(게임/소프트/리니지 등)' 있을 때만 인정. 수집 시 기존에 쌓인 본사 노이즈도 자동 삭제(정리).
- **v0.40 · build 260924** — 대량 수집 폭주/멈춤/‘불러오기 실패’ 해결. 새 기사가 많으면(>150) 원문 해석(batchexecute) 생략하고 RSS 요약만 사용(구글 차단·지연 회피). 그룹화는 대량(>600)일 때 N×N 유사도 대신 제목 기준(O(N))으로 → 메모리 초과(OOM) 방지.
- **v0.39 · build 260924** — 버전 넘버링 리마스터(vNN → v0.NN + build 날짜). 저작권 김상화. 개발노트 상세 재작성 + 앱 내 노트 마크다운 렌더.
- **v38** — 푸터를 **항상 보이는 얇은 고정 바**로(하단 고정, 스크롤 무관). 저작권(© 2026 kksh0379)·버전·저장상태 표시. 본문 하단 여백/맨위로 버튼 위치 조정.
- **v37** — **푸터 추가**(앱명·설명·버전·저장상태·출처). 버전 표기를 헤더에서 **푸터로 이동**, 헤더는 제목+버튼만.
- **v36** — 관리자 비번을 **입력창 자동채움**용으로만 캐시(자동 로그인은 안 함). 로그인 창 열면 저장된 비번이 채워져 매번 타이핑 불필요.
- **v35** — 뉴스(기사) 탭 범례를 **관리자 전용**으로(게시판·소셜 범례는 공개 유지). 관리자 **비밀번호 로컬 저장→자동 로그인**(매번 입력 불필요, 로그아웃 시 저장 비번 삭제).
- **v34** — **구글 100건 제한 우회**: 수집 기간을 120일 구간으로 쪼개 `after:`/`before:` 날짜검색으로 각 구간 수집 → **과거(최대 5년)까지 깊게** 수집. UI: 필터 선택 시 **로딩 표시**, **맨 위로 플로팅 버튼**, 목록 **무한 스크롤**. 조회 상한 상향(뉴스 1000/게시판·소셜 500).
- **v33** — '수집 대기' 멈춤 방지(URL만 경량 로드+멈춘 작업 30분 후 자동 해제), 수집 기간 드롭다운을 **관리자 전용**(수동수집 세트)으로. 개발노트/패치내역(관리자 전용 📓 노트).
- **v32** — 정기 배치 수집 창을 2년→**최근 30일**로 축소(최초 5년 백필과 역할 분리). `BATCH_DAYS` 환경변수 추가.
- **v31** — **관리자 로그인 부활**(비밀번호 로그인, 세션): 로그인해야 상태확인·수집 버튼 노출·동작. 수집 기간 **5년 옵션** 추가. **빈 DB 자동 백필**(앱 시작 시 비어 있으면 5년치 자동 수집 → 배포하면 버튼 없이 기사 표시).
- **v30** — **Neon 연결 안정화**: `channel_binding` 제거 + 연결 풀 견고화(cold start/유휴 끊김 대응). "couldn't get a connection" 해결.
- (설정) **외부 Postgres(Neon) 연결**: `DATABASE_URL` 등록 → 데이터 영구 저장. **cron-job.org + `CRON_TOKEN`** 로 정기 자동 수집.

## 2026-09-23
- **v29** — 자동 수집(외부 크론) + 상태확인/수집 **관리자 전용화**(당시 키 방식, 이후 v31에서 로그인으로 교체). `/api/cron` GET/POST·비차단.
- **v28** — 저장소 종류 배지(☁ 영구 / ⚠ 임시) 표시로 DB 연결 확인 용이.
- **(DB)** — SQLite/Postgres 이중 지원(`DATABASE_URL` 있으면 Postgres). ON CONFLICT 배치 저장.
- **v27** — 뉴스 **수집 기간 선택**(1/3/6개월·1/2년, 기본 3개월).
- **v25~v26** — 뉴스 **재단/본사 카테고리** + 셀렉트, 본사=엔씨소프트+**자회사**(NC AI/QA/IDS·게임스튜디오), 야구 제외. 화면 꺼짐/백그라운드에도 수집 유지(서버 백그라운드 작업 + 폴링).
- **v24** — 본사 키워드 정제(NC/엔씨 단어 단위 매칭 + 야구 노이즈 제외).
- **v22~v23** — 100개 제한 제거(전량 수집), 속도 최적화(병렬 24·짧은 타임아웃), 증분 수집.
- **v19~v21** — 뉴스 증분 수집(저장키=구글 링크 고정) + 진행률 실시간 표시, 원문 URL 분리(source_url).
- **v16~v18** — 동일 기사 그룹화(아코디언), 최근 N년 필터, 진행상황 스트리밍.
- **v13~v15** — 관리자 로그인 제거(당시) + 모바일 셀렉트 UI 수정(커스텀 드롭다운), 상태확인에 "추출 글 수" 표시.
- **게시판 수정** — 프로젝토리 SSL 인증서 우회(verify 폴백) + 실제 선택자 적용, `/api/inspect` API 헌터/probe 추가(SPA 내부 API 탐색).
- **뉴스 본문** — 구글 뉴스 신형 URL 디코더(batchexecute)로 원문 복원, 실패 시 RSS 요약 폴백.

## 2026-06 (최초 구축)
- Flask 앱 초안: 탭 3개(뉴스·게시판·소셜), 구글 뉴스 RSS 수집, 게시판 크롤러, 소셜(유튜브 RSS), SQLite 저장, 중복 판단(TF-IDF), Render 배포 설정.
