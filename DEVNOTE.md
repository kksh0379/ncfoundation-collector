# 🛠 개발 노트 — 휴 스코프

> 📌 **고정 메모 (베타 공개 전 할 것)**
> - **진단 엔드포인트 잠그기**: `/api/peek`, `/api/apihunt`, `/api/grep`, `/api/grepall`, `/api/dbcheck` 가 현재 공개 상태다. peek/grep 계열은 우리 서버로 임의 URL을 대신 요청하는 오픈 프록시라 악용 위험 → 관리자 전용(`_admin_ok`)으로 잠그거나 제거할 것.
> - FAIR AI 인사이트는 미수집(요구사항은 공지사항만). 필요해지면 브라우저 네트워크 탭에서 인사이트 로딩 시 호출되는 API를 확인해 붙이면 됨.

휴 스코프의 구조·운영·설정을 정리한 문서입니다. 엔씨문화재단과 엔씨소프트(및 자회사) 관련 **뉴스·서비스 게시판·소셜 채널**을 자동으로 수집해 모바일 웹으로 보여주는 서비스입니다.

---

## 0. 한눈에 보기

- **앱**: 파이썬 Flask 하나로 동작(웹 화면 + 수집기).
- **호스팅**: Render (웹 서비스).
- **데이터베이스**: 외부 Postgres(`DATABASE_URL`). 서버가 재시작돼도 데이터가 유지됨.
- **정기 수집**: 외부 크론(cron-job.org)이 주기적으로 앱을 호출해 수집 실행.
- **출처**: 구글 뉴스 RSS(뉴스), 각 기관 사이트(게시판), 유튜브/인스타(소셜).

---

## 1. 전체 구조

- **방문자**는 웹에서 뉴스/게시판/소셜을 조회한다(보기 전용).
- **Flask 앱**이 실제로 각 출처를 크롤링해 수집하고 화면을 렌더링한다.
- 수집한 데이터는 **Postgres**에 저장하고, 화면 표시도 여기서 읽는다.
- **크론**이 정해진 주기로 앱의 수집 엔드포인트를 호출해 새 글을 보충한다.

무료/저비용으로 상시 운영하기 위해, 앱·DB·정기실행을 분리해 각각 안정적인 방식으로 구성했다.

---

## 2. 외부 서비스

### Render — 호스팅
- 대시보드: dashboard.render.com / 서비스 URL: `https://ncfoundation-collector.onrender.com`
- GitHub 저장소(`kksh0379/ncfoundation-collector`)와 연동되어, 코드를 push하면 자동으로 빌드·배포된다.
- 배포 브랜치는 `claude/quirky-euler-agfmp`. 이 브랜치에 올라간 코드가 배포된다.
- 시작 명령: `gunicorn app:app --workers 1 --threads 4 --timeout 300` (Procfile / render.yaml).
- 무료 플랜은 15분간 트래픽이 없으면 앱이 잠들어(spin down) 첫 접속이 느리다. 상시 운영을 위해 유료(Starter) 인스턴스를 쓰면 잠들지 않는다.

### 데이터베이스 — Postgres
- 연결 문자열을 환경변수 `DATABASE_URL`에 넣으면 Postgres로 동작하고, 없으면 로컬 SQLite(임시)로 동작한다.
- 앱은 연결 시 `channel_binding` 파라미터를 제거하고 `sslmode=require`를 보장한다(일부 풀러 조합에서의 연결 실패 예방).
- 연결은 커넥션 풀 대신 **요청마다 직접 접속**하는 방식을 쓴다. 트래픽이 적은 서비스에서 더 단순하고 안정적이다.
- 무료 등급 DB는 유휴 시 컴퓨트가 잠들 수 있다(cold start). 앱이 주기적으로 가벼운 쿼리를 보내 깨워 두는 keep-alive가 있다(`DB_KEEPALIVE_SEC`, 기본 240초).
- 화면 하단 배지로 상태를 표시한다: **☁ 영구저장**(Postgres 연결됨) / **⚠ 임시저장**(SQLite) / **⛔ DB 연결 안 됨**.

### cron-job.org — 정기 수집
- 콘솔: console.cron-job.org
- 앱의 `/api/cron?token=<CRON_TOKEN>` 를 주기적으로 호출해 백그라운드 수집을 트리거한다.
- `<CRON_TOKEN>` 은 환경변수 `CRON_TOKEN` 값과 일치해야 한다. 정상 호출 시 `{"ok": true, "started": [...]}` 를 반환한다.

---

## 3. 화면 구성 / 권한

- 탭 3개: **뉴스·기사 / 서비스 게시판 / 소셜 채널**.
- 상단 헤더에 BI(NC 스코프 로고, 클릭 시 홈 이동)와 관리자 버튼이 있다.
- **관리자 로그인**(비밀번호, `ADMIN_PW`) 시에만 상태 확인·수집 실행·DB 초기화·개발노트가 노출·동작한다. 일반 방문자는 조회만 가능하다.
- 로그인 편의를 위해 비밀번호를 브라우저에 저장해 로그인 창에서 자동 채움 처리한다(자동 로그인은 아님).
- 하단 푸터는 항상 보이는 얇은 고정 바로, 저작권·버전·저장 상태를 표시한다.
- **보기 방식**: 리스트(제목만 한 줄) / 카드(썸네일·요약)를 선택할 수 있고, 선택은 브라우저에 저장된다. 기본은 리스트.

---

## 4. 환경변수 (Render → Environment)

- **`DATABASE_URL`** — Postgres 연결 문자열. 있어야 데이터가 영구 저장된다.
- **`CRON_TOKEN`** — 외부 크론 인증 토큰. cron-job.org URL의 `token=` 값과 동일해야 한다.
- **`ADMIN_PW`** — 관리자 로그인 비밀번호. 미설정 시 코드 기본값 사용(운영에서는 설정 권장).
- **`SECRET_KEY`** — 로그인 세션 서명 키(긴 무작위 문자열 권장).
- **`BACKFILL_DAYS`** — 최초 자동 백필 기간(일). 기본 `1825`(5년).
- **`BATCH_DAYS`** — 정기 배치의 뉴스 수집 창(일). 기본 `30`.
- **`AUTO_BACKFILL`** — 앱 시작 시 DB가 비어 있으면 자동 수집할지. 기본 꺼짐(`0`). 대량 백필이 부팅 부하를 유발할 수 있어 기본은 끔.
- **`ENABLE_SCHEDULER`** — 앱 내부 스케줄러 사용 여부. 기본 켜짐(`1`).
- **`DB_KEEPALIVE_SEC`** — DB keep-alive 주기(초). 기본 `240`, `0`이면 끔.
- **`IMG_ENRICH_MAX`** — 수집 1회당 대표 이미지 보강 개수 상한. 기본 `200`.
- **`YOUTUBE_API_KEY`** — 있으면 유튜브 전 영상(과거 포함)을 Data API로 수집, 없으면 RSS(최신 ~15개)로 폴백. (선택)

> 비밀값(DB 연결 문자열·비밀번호·토큰)은 코드나 저장소가 아니라 환경변수에만 둔다.

---

## 5. 수집 동작

1. **최초 자동 백필** — DB가 비어 있을 때만(옵션, 기본 꺼짐) 최근 `BACKFILL_DAYS`(기본 5년)를 수집한다.
2. **정기 배치** — 내부 스케줄러와 외부 크론이 최근 `BATCH_DAYS`(기본 30일)만 훑어 새 글을 보충한다.
3. **수동 수집** — 관리자가 "수집 실행"을 누르면 뉴스는 최근 5년(`RECENT_DAYS`) 기준으로 수집한다.

공통:
- **중복 방지**: 저장 시 URL 기준으로 이미 있는 글은 건너뛴다(신규 수는 저장 전후 개수 차이로 계산).
- **멈춤 방지**: 작업이 30분 넘게 진행 중이면 멈춘 것으로 간주해 자동 해제한다.
- **진행 표시**: 수집은 서버 백그라운드에서 돌고, 화면은 상태를 폴링해 단계별 진행을 보여준다. 브라우저를 닫거나 화면이 꺼져도 서버 수집은 계속된다.

---

## 6. 뉴스 수집 상세

- **카테고리**: 재단(`엔씨문화재단`, `NC문화재단`) / 본사(`엔씨소프트`, `NCSOFT`, `NC`, `엔씨` + 자회사 `엔씨에이아이(NC AI)`, `엔씨QA`, `엔씨IDS`, `퍼스트스파크 게임즈`, `빅파이어 게임즈`, `루디우스 게임즈`).
- **노이즈 제외**: 야구단(NC 다이노스), 백화점(NC백화점=뉴코어) 등 무관 기사는 제외. 모호한 `NC`/`엔씨`는 게임 회사 문맥어가 있을 때만 인정.
- **깊은 과거 수집**: 구글 뉴스 RSS는 요청당 약 100건, 최신 위주로만 준다. 이를 우회하려고 기간을 120일 구간으로 나눠 `after:`/`before:` 날짜 검색으로 각 구간을 수집한다.
- **본문/이미지**: 구글 링크는 리다이렉트라 원문 URL을 복원(batchexecute)해 본문 요약과 대표 이미지(og:image)를 추출한다. 실패 시 RSS 요약을 사용한다. 저장 키(url)는 구글 링크로 고정하고 실제 기사 주소는 `source_url`에 따로 둔다.
- **대량 처리**: 신규가 많으면(>150) 속도를 위해 원문 해석을 생략하고 RSS 요약만 저장한다. 대표 이미지는 이후 수집 때마다 최근 글부터 조금씩 보강한다.
- **동일 기사 묶음**: 제목이 같거나, 제목이 달라도 본문 유사도(글자 n-gram TF-IDF 코사인)가 높으면 하나로 묶는다. 대량에서도 안전하도록 같은 날짜 블록 안에서 비교한다. 화면에서는 대표 기사 한 건과 "N개 매체" 아코디언으로 표시한다.

---

## 7. 게시판/소셜 현황

- **나의AAC**: 정상 수집.
- **프로젝토리**: SSL 인증서 이슈 우회 + 실제 선택자 적용으로 수집.
- **FAIR AI · 대표 홈페이지**: 목록을 JavaScript로 렌더하는 SPA라 정적 HTML에 글이 없어 구현 예정(내부 API 연동 필요).
- **유튜브**: 채널 RSS로 수집.
- **인스타그램**: 로그인 없는 공개 API로 시도하나 데이터센터 IP에서는 차단될 수 있음(공식 Graph API 토큰 필요).

---

## 8. 자주 겪는 문제

- **첫 접속이 느림/502**: 무료 플랜에서 앱이 잠들었다 깨는 중. 유료 인스턴스면 발생하지 않음.
- **⚠ 임시저장 배지**: `DATABASE_URL` 미반영. 환경변수 확인 후 재배포.
- **DB 연결 실패**: DB 컴퓨트가 잠들었거나 연결 문자열 불일치. `/api/dbcheck`(관리자)로 실제 원인 확인 가능.
- **수집이 진행되지 않음**: 30분 후 자동 해제됨. 지속되면 Render Logs에서 `[crawl]`/`[google]` 로그 확인.
- **과거 기사가 얕게 나옴**: 구글 RSS의 과거 색인 한계. 구간을 더 잘게 나누면 개선되나 느려짐.

---

## 9. 데이터 보존 주의

- `DATABASE_URL` 을 삭제하면 임시 SQLite로 돌아가 재시작 시 초기화된다.
- DB 콘솔에서 프로젝트/DB를 삭제·리셋하지 않는다.
- 관리자 "DB 초기화"는 수집 데이터(뉴스·게시판·소셜)를 모두 지운 뒤 다시 수집한다. 되돌릴 수 없다.

---

## 10. 링크

- 사이트: https://ncfoundation-collector.onrender.com
- 저장소: https://github.com/kksh0379/ncfoundation-collector
- Render: https://dashboard.render.com
- cron-job.org: https://console.cron-job.org

---

## 11. 코드 맵

- `app.py` — 라우트(화면·API), 로그인, DB 보장/keep-alive, 수집 작업 실행·상태, 스케줄러/크론/백필, DB 초기화.
- `collector/google_news.py` — 뉴스 수집(카테고리·키워드·구간 분할·본문/이미지 복원·필터).
- `collector/boards.py` — 게시판 수집(사이트별 선택자).
- `collector/social.py` — 유튜브/인스타 수집.
- `collector/dedup.py` — 동일 기사 그룹화.
- `collector/db.py` — 저장소(Postgres/SQLite), 직접 접속.
- `collector/fetcher.py` — HTTP 요청 헬퍼.
- `collector/extractor.py` — 본문·대표 이미지 추출.
- `templates/index.html`, `static/js/app.js`, `static/css/style.css` — 화면.
- `DEVNOTE.md`(이 문서), `CHANGELOG.md`(변경 이력).
