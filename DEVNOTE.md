# 🛠 개발노트 (나중의 나를 위한 메모)

> 이건 내가 이 프로젝트를 몇 달 뒤에 다시 열었을 때 "아 맞다, 이거 이렇게 굴러갔지" 하고 바로 떠올리려고 쓰는 메모다. 남한테 설명하는 문서가 아니라 내 노트니까, 삽질한 이유랑 왜 이렇게 만들었는지까지 솔직하게 적어둔다. 값·경로·동작은 나중에 헷갈리니까 구체적으로 다 박아둔다.

---

## 0. 한 줄 정리

엔씨문화재단 관련 **뉴스·서비스 게시판·소셜 채널**을 자동으로 긁어서 모바일 웹으로 보여주는 수집기다. 본체는 **파이썬 Flask** 하나. 이걸 **Render**에 올려서 띄웠고, 긁은 데이터는 **Neon(외부 Postgres)** 에 저장해서 서버가 껐다 켜져도 안 날아가게 했다. **cron-job.org** 가 주기적으로 내 앱을 대신 눌러줘서(=깨워서) 자동 수집이 돌게 해놨다.

---

## 1. 전체 구조 — 내가 어떻게 나눠놨나

머릿속에 이 그림 하나면 된다.

- **방문자(브라우저)** 는 들어와서 뉴스/게시판/소셜을 **보기만** 한다.
- **Render** 위에 내 **Flask 앱**이 떠 있다. 얘가 실제로 인터넷 돌아다니며 기사 긁고, 화면 그려준다.
- 긁은 데이터는 **Neon(Postgres)** 에 저장하고, 화면 보여줄 때도 여기서 꺼낸다.
- **cron-job.org** 가 바깥에서 주기적으로 내 앱의 특정 주소를 호출해서, 잠든 앱을 깨우고 "수집 한 번 돌려" 시킨다.
- 기사 출처는 **구글 뉴스 RSS**(뉴스), **각 기관 사이트**(게시판), **유튜브/인스타**(소셜).

왜 이렇게 쪼갰냐면 — 무료로 오래 굴리려고. Render 무료는 앱만 띄워주고 데이터는 안 지켜준다(재시작하면 초기화). 그래서 데이터는 Neon으로 빼서 지키고, 무료라 자꾸 잠드는 건 cron이 깨워주는 걸로 때웠다.

---

## 2. 외부 서비스 3개 — 뭘·왜·어디에 세팅했나 (제일 중요)

### Render — 앱 띄우는 곳 (호스팅)
- 주소: dashboard.render.com / 실제 URL은 `https://ncfoundation-collector.onrender.com`
- GitHub 저장소(`kksh0379/ncfoundation-collector`)랑 연결해놔서, 코드를 push하면 Render가 받아서 다시 빌드·배포한다.
- **배포 브랜치 주의(내가 여기서 헤맸음)**: Render는 특정 브랜치 하나만 본다. 지금 배포용은 `claude/quirky-euler-agfmp` 브랜치다. 다른 브랜치에 아무리 push해도 사이트엔 안 올라간다 — 한 번 이걸로 "배포했는데 버전이 안 바뀌네?" 하고 삽질했다. 새 작업도 결국 이 브랜치에 올려야 배포된다.
- **무료 플랜 특징 2가지**:
  1. 약 **15분간 아무도 안 들어오면 잠든다**(spin down). 그 뒤 처음 들어오면 깨는 데 **30~50초**. 그래서 "페이지가 작동하지 않습니다" 떠도 1~2분 뒤 새로고침하면 대개 열린다.
  2. 잠들면 앱 안 스케줄러도 같이 멈춘다. 그래서 확실한 정기 수집은 바깥 cron으로 해결.
- 실제로 내가 만진 건 **환경변수(Environment)** → 4번 참고.
- 배포 확인: 화면 아래 푸터 버전이 올라갔는지 보면 된다. 자동배포 안 되면 Render에서 **Manual Deploy → Deploy latest commit**.

### Neon — 데이터 저장하는 곳 (Postgres DB)
- 주소: console.neon.tech
- 여기가 **핵심**. 수집한 기사가 전부 여기 저장돼서 Render가 재배포·재시작돼도 **데이터가 유지**된다.
- 내가 한 것: Neon 프로젝트(`kksh0379`) 만들고, 거기서 준 **연결 문자열**을 복사해서 Render 환경변수 `DATABASE_URL`에 붙였다.
  - 문자열 모양: `postgresql://사용자:비번@호스트-pooler.../neondb?sslmode=require&channel_binding=require`
  - 앱이 연결할 때, Neon pooler랑 궁합 안 좋은 `channel_binding`은 코드가 자동으로 떼고 `sslmode=require`로 강제한다. 그래서 문자열 그대로 붙여도 된다.
  - Neon 무료도 안 쓰면 compute가 잠들어서(cold start) 첫 연결이 느리다. 그래서 연결 풀을 "유휴 연결 안 붙잡고(min_size=0), 쓸 때 살아있는지 확인, 연결 대기 45초 + 실패 시 최대 3회 재시도"로 해놨다. 예전에 "couldn't get a connection after 30s" 계속 나던 거 이걸로 잡았다.
- **연결 확인법**: 푸터에 **☁ 영구저장**(초록) 뜨면 Neon 성공. **⚠ 임시저장**(주황)이면 아직 SQLite(임시)라 재시작 때 날아간다.
- **비번 바꾼 적 있음**: 연결 문자열이 노출돼서 Neon에서 비번 Reset하고 새 문자열로 Render `DATABASE_URL` 교체했다. 앞으로도 비번 바꾸면 → Neon에서 새 문자열 복사 → Render `DATABASE_URL`만 교체 → 저장(자동 재배포) 순서.

### cron-job.org — 주기적으로 자동 수집시키는 곳
- 주소: console.cron-job.org
- 무료 Render는 잠들면 스스로 수집 못 하니, 바깥에서 주기적으로 깨우고 수집시키는 역할.
- 내가 한 것: cronjob 하나 만들었다.
  - 호출 URL: `https://ncfoundation-collector.onrender.com/api/cron?token=<CRON_TOKEN>`
  - 주기: 예를 들어 6시간마다. (Neon cold start 줄이려면 더 짧게 해도 됨)
  - `<CRON_TOKEN>` 은 아무나 이 주소 못 부르게 막는 비밀 토큰. Render 환경변수 `CRON_TOKEN` 값이랑 글자까지 똑같아야 한다. (실제 값은 Render에만 있음, 여기 안 적음)
- **작동 확인법**: 그 URL을 브라우저로 열어서 `{"ok": true, "started": [...]}` 나오면 정상. `started`가 `[]`면 "이미 수집 중이라 새로 안 띄움"이라 이것도 정상. `{"error":"unauthorized"}` 나오면 토큰 안 맞는 거.

---

## 3. 로그인/화면 구조

- 탭 3개: **뉴스·기사 / 서비스 게시판 / 소셜 채널**.
- 위쪽 **관리자 로그인** 버튼. 비번은 `ADMIN_PW` 환경변수 값(설정 안 하면 코드 기본값).
  - 로그인해야만 **상태 확인 · 수집 실행 · 수집 기간 선택 · DB 비우기 · 📓 노트(이 문서)** 가 보이고 동작한다. 일반 방문자는 **보기만**.
  - 편의로, 로그인 성공하면 비번을 브라우저에 저장했다가 다음에 로그인 창 열면 **자동으로 채워준다**(자동 로그인 아니고 버튼만 누르면 됨). 로그아웃하면 세션은 끊기지만 저장된 비번은 자동채움용으로 남는다.
- 아래 **푸터**는 항상 보이는 얇은 바. 저작권(© 2026 김상화)·버전·저장상태(☁/⚠).

---

## 4. 환경변수 (Render → Environment) — 제일 자주 볼 부분

Render 서비스 화면 상단 메뉴(모바일은 `Deploys ▾` 드롭다운) → **Environment**. 각 항목 의미:

- **`DATABASE_URL`** — Neon 연결 문자열. **이게 있어야 영구 저장**. 지우면 임시 SQLite로 돌아가 재시작 때 초기화. (설정함)
- **`CRON_TOKEN`** — 외부 크론이 `/api/cron` 부를 때 쓰는 비밀 토큰. cron-job.org URL의 `token=`이랑 똑같아야 함. (설정함)
- **`ADMIN_PW`** — 관리자 로그인 비번. 설정 안 하면 코드 기본값. 운영에선 여기서 바꾸는 게 낫다.
- **`SECRET_KEY`** — 로그인 세션 서명값. 긴 무작위 문자열 넣어두면 더 안전. (권장)
- **`BACKFILL_DAYS`** — 최초 자동 수집(백필) 기간. 기본 `730`(=2년). 더 깊게 긁고 싶으면 `1825`(5년) 등으로 올림. (선택)
- **`BATCH_DAYS`** — 정기 배치가 훑는 기간. 기본 `30`(일). (선택)
- **`AUTO_BACKFILL`** — 앱 시작 시 DB 비었으면 자동 백필할지. 기본 켜짐(`1`). `0`이면 끔. (선택)
- **`ENABLE_SCHEDULER`** — 앱 내부 4시간 스케줄러 on/off. 기본 켜짐(`1`). (선택)

> 🔐 비밀값(DB 연결문자열·비번·토큰)은 **코드·GitHub·채팅에 절대 안 적고** 오직 Render 환경변수에만 둔다. 이 노트에도 이름만 적었다.

---

## 5. 수집 도는 3가지 경로

1. **최초 자동 백필 (딱 한 번, 깊게)**
   - 앱이 처음 떴는데 뉴스 DB가 **완전히 비어 있을 때만** 자동 실행.
   - 뉴스는 **최근 2년치**(BACKFILL_DAYS), 게시판·소셜도 같이. (원래 5년이었는데 너무 오래 걸리고 그 사이 Neon이 자다 깨는 구간이 길어져서 cold start·메모리 초과가 나길래 2년으로 줄였다. 뉴스 필터 상한 RECENT_DAYS=730이랑도 맞춰짐.)
   - 한 번 채워지면 다음부턴 "데이터 있음 → 건너뜀". 그래서 배포할 때마다 다시 안 긁는다.
   - 이거 덕에 **배포만 해두면 버튼 안 눌러도 기사가 채워져** 방문자에게 바로 보인다.

2. **정기 배치 (주기적, 최신만)**
   - 앱 내부 스케줄러(4시간마다) + 바깥 cron-job.org 호출.
   - 뉴스는 **최근 30일**(BATCH_DAYS)만 훑어서 새 기사 보충. (2년치를 매번 다시 긁으면 낭비라 역할 나눔.)

3. **수동 (내가 직접)**
   - 로그인해서 **수집 실행** 버튼. 기간 선택 UI는 없앴고 항상 **최근 2년**을 긁는다(서버 기본값 RECENT_DAYS=730). 기간을 바꾸고 싶으면 코드의 RECENT_DAYS나 환경변수를 만지면 된다.

### 공통 규칙
- **중복 방지**: 저장할 때 `url`(구글 링크) 기준 ON CONFLICT로 이미 있는 기사는 건너뛴다(새 기사만 추가). 신규 건수는 저장 전후 COUNT 차이로 센다. (예전엔 수집 전에 '기존 URL 전체'를 메모리로 끌어와 비교했는데, 테이블이 크고 Neon이 자다 깰 때 이 "기존 데이터 확인" 단계가 너무 느려서 없앴다.)
- **멈춤 방지**: 어떤 작업이 30분 넘게 "실행 중"이면 멈춘 걸로 보고 자동 해제. 몇 시간씩 갇히지 않게.
- **진행 표시**: 서버에서 백그라운드로 돌고, 화면은 상태를 폴링해서 "RSS N/총 구간 → 요약 N/M → 저장·그룹화 → 완료"로 단계가 흐른다. **폰 화면 꺼도 서버 수집은 계속**되고, 돌아오면 자동으로 상태에 다시 붙는다.

---

## 6. 뉴스 수집 핵심 디테일 (여기가 제일 까다로웠음)

- **카테고리 2개**:
  - **재단** = `엔씨문화재단`, `NC문화재단`
  - **본사** = `엔씨소프트`, `NCSOFT`, `NC`, `엔씨` + 자회사 `엔씨에이아이(NC AI)`, `엔씨QA`, `엔씨IDS`, `퍼스트스파크 게임즈`, `빅파이어 게임즈`, `루디우스 게임즈`
  - `NC`/`엔씨` 는 영어 단어 속 'nc'(announce 등)나 야구단 **NC 다이노스**, **NC백화점(뉴코어)** 을 마구 잡아온다. 그래서 영문 키워드는 "단어 단위"로만 매칭하고, `다이노스·프로야구·야구·KBO·구단`, 백화점 관련어가 든 기사는 제외한다. 모호한 NC/엔씨는 게임회사 문맥어(게임/소프트/리니지 등) 있을 때만 인정.
- **왜 5년치가 잘 안 나왔었나**: 구글 뉴스 RSS는 **한 요청에 최대 ~100건, 최신 위주**로만 준다. 그래서 `최근 5년`으로 요청해도 구글이 100건에서 잘라버린다(기사 많은 본사는 몇 달치만 나옴).
  - **해결**: 기간을 **120일 구간으로 쪼개서**(`after:날짜 before:날짜`) 각 구간마다 100건씩 받는다. 그러면 과거까지 깊게 긁힌다. 키워드(13개)×구간을 병렬로 돌린다.
  - 그래도 아주 오래된 기사는 구글 색인이 적어서 다 못 채울 수 있다(특히 재단). 구간을 더 잘게 쪼개면 촘촘해지는데 느려진다.
- **본문(요약)**: 구글 뉴스 링크는 리다이렉트라 원문 URL을 바로 못 얻는다. 그래서 구글 내부 API(batchexecute)로 실제 주소 복원해서 본문 뽑고, 실패하면 RSS 요약(snippet) 사용. **저장 키(url)는 항상 구글 링크로 고정**(그래야 다음 수집 때 "이미 있음"으로 인식돼 증분됨), 복원한 실제 주소는 `source_url`에 따로 넣어서 "원문 보기"에 쓴다.
- **새 기사 많을 때(>150)** 는 원문 해석(batchexecute) 생략하고 RSS 요약만 쓴다(구글 차단·지연 회피).
- **동일 기사 묶음(아코디언)**: 같은 보도자료가 여러 매체에 뿌려지면 제목/본문 유사도(글자 n-gram TF-IDF 코사인, 임계값 0.55)로 묶어서 `group_key`를 매긴다. 대량(>600)일 땐 N×N이 메모리 터져서(OOM) 제목 기준(O(N))으로만 묶는다. 화면엔 대표 하나만 카드로 보이고 "같은 기사 N건" 누르면 펼쳐진다.

---

## 7. 게시판/소셜 상태

- **나의AAC**: 정상(서버가 HTML 그대로 주는 사이트라 선택자로 잘 긁힘).
- **프로젝토리**: 예전엔 SSL 인증서 체인 문제로 접속 자체가 실패했는데 "SSL 검증 실패 시 검증 끄고 재시도"로 뚫었고, 실제 목록 선택자(`a.info__title-link` 등) 넣었다.
- **FAIR AI · 대표 홈페이지**: 목록을 JS로 그리는 SPA라 정적 HTML엔 목록이 없다. 그래서 지금은 "구현 예정". 내부 JSON API 찾아 붙이거나 헤드리스 브라우저 필요. (`/api/inspect`에 API 후보 찾는 도구 넣어둠.)
- **유튜브**: 채널 RSS로 최신 영상 정상.
- **인스타그램**: 로그인 없는 공개 API로 시도하는데, 데이터센터 IP(무료 호스팅)는 인스타가 자주 막아서 0건일 수 있다. 확실히 하려면 공식 Graph API 토큰 필요.

---

## 8. 자주 겪는 문제 & 해결

- **"페이지가 작동하지 않습니다"** → 무료 Render가 자다 깨는 중(30~50초). 1~2분 뒤 새로고침.
- **배포했는데 버전이 안 바뀜** → 배포 브랜치(`claude/quirky-euler-agfmp`)가 아닌 딴 브랜치에 올렸을 가능성. 이 브랜치에 올려야 함. 아니면 브라우저 캐시 → 강력 새로고침.
- **푸터가 ⚠ 임시저장** → `DATABASE_URL` 반영 안 됨. Render 환경변수 확인 + 재배포 기다리기.
- **수집이 "couldn't get a connection"** → Neon 연결 실패(주로 cold start). 재배포/연결문자열 확인. 코드가 재시도는 자동으로 함. 데이터 양을 줄이면(2년치) 이 창이 짧아져서 덜 남.
- **`/api/cron`이 unauthorized** → URL token과 Render `CRON_TOKEN` 다름. 값 일치 + 재배포 확인.
- **"수집 대기"에서 안 움직임** → 30분 지나면 자동으로 "중단됨" 풀림. 새로고침 후 다시 수집. 계속 그러면 Render Logs에서 `[crawl]`/`[backfill]`/`[google]` 줄 봐야 함.
- **기사가 과거까지 안 내려감** → 구글 RSS 한계(오래된 색인 부족). 구간을 더 잘게(예: 60일) 하면 개선되지만 느려짐.
- **DB가 이상한 데이터로 꼬였을 때** → 관리자 화면 **🗑 DB 비우기**로 싹 지우고 최근 2년치로 다시 수집(9번 참고).

---

## 9. 🗑 DB 비우기 기능 (관리자)

- 관리자로 로그인하면 뉴스 탭에 **🗑 DB 비우기** 버튼이 보인다.
- 누르면 확인창 뜨고, 확인하면 **뉴스·게시판·소셜 데이터를 전부 삭제**한 뒤 **곧바로 최근 2년치(현재 선택된 수집 기간)로 새로 수집**을 시작한다.
- 만든 이유: 초기 수집이 계속 실패하면서 반쪽짜리·이상한 데이터가 쌓였는데, 증분 수집이라 "이미 있음"으로 인식해서 그 쓰레기가 안 지워졌다. 그래서 통째로 비우고 깨끗하게 다시 받을 수단이 필요했다.
- 내부적으론 `POST /api/admin/purge` (관리자 전용). body의 `scope`(all/news/boards/social)로 대상 고르고, `recollect:true`면 비운 뒤 재수집까지 한 번에. `meta`(마지막 수집 일시 등)는 안 지운다.
- ⚠ 되돌릴 수 없다. 진짜 꼬였을 때만.

---

## 10. ⛔ 데이터 날아가는 행동 (절대 금지)

1. Render에서 **`DATABASE_URL` 삭제** → 임시 SQLite로 돌아가 초기화.
2. **Neon 콘솔에서 DB/프로젝트 삭제·리셋**.

이 둘만 안 건드리면 영구 보존된다. 배포·재시작은 안전. (DB 비우기 버튼은 데이터는 지우지만 저장소 연결은 그대로.)

---

## 11. 링크 모음
- 사이트: https://ncfoundation-collector.onrender.com
- GitHub: https://github.com/kksh0379/ncfoundation-collector
- Render(호스팅): https://dashboard.render.com
- Neon(DB): https://console.neon.tech
- cron-job.org(정기 실행): https://console.cron-job.org

---

## 12. 코드 어디에 뭐가 있나 (수정할 때 참고)
- `app.py` — Flask 앱. 라우트(화면·API), 로그인, DB 비우기, 수집 작업 실행/상태, 스케줄러/크론/백필.
- `collector/google_news.py` — 뉴스 수집(카테고리·키워드·구간 분할·본문 복원·필터).
- `collector/boards.py` — 게시판 수집(사이트별 선택자).
- `collector/social.py` — 유튜브/인스타 수집.
- `collector/dedup.py` — 동일 기사 묶기(group_key).
- `collector/db.py` — 저장소. `DATABASE_URL` 있으면 Postgres, 없으면 SQLite. `clear_tables()`가 DB 비우기.
- `collector/fetcher.py` — HTTP 요청 헬퍼(타임아웃·SSL 폴백 등).
- `templates/index.html`, `static/js/app.js`, `static/css/style.css` — 화면.
- `DEVNOTE.md`(이 노트), `CHANGELOG.md`(일자별 패치 내역).
