# 🛠 개발노트 — 김상화님을 위한 상세 안내

이 문서는 나중에 김상화님이 이 프로젝트를 다시 열었을 때, **"아 맞다, 이게 이렇게 돌아갔지"** 하고 바로 기억나도록 최대한 자세히, 그리고 옆에서 설명하듯 적은 노트예요. 모르는 용어가 나오면 편하게 검색해 보시면 됩니다. 디테일이 빠지면 나중에 곤란하니, 값·경로·동작을 구체적으로 다 적어뒀어요.

---

## 0. 한 문단 요약

엔씨문화재단 관련 **뉴스·서비스 게시판·소셜 채널**을 자동으로 긁어와서 모바일 웹으로 보여주는 수집기예요. 앱 본체는 **파이썬 Flask** 하나이고, 이걸 **Render**라는 호스팅에 올려서 인터넷에 띄웠어요. 긁어온 데이터는 **Neon**이라는 외부 데이터베이스(Postgres)에 저장해서, 서버가 껐다 켜져도 데이터가 안 날아가요. 그리고 **cron-job.org**가 정해진 시간마다 우리 앱을 대신 눌러줘서(=깨워서) 자동으로 수집이 돌아가게 해놨어요.

---

## 1. 전체 구조 — 누가 무슨 일을 하나

머릿속에 이 그림 하나만 있으면 돼요.

- **사람(브라우저)** 은 사이트에 들어와서 뉴스/게시판/소셜을 **보기만** 해요.
- **Render** 위에 우리 **Flask 앱**이 떠 있어요. 이 앱이 실제로 인터넷을 돌아다니며 기사를 긁고(수집), 화면을 그려줘요.
- 앱은 긁은 데이터를 **Neon(Postgres DB)** 에 저장하고, 화면을 보여줄 때도 여기서 꺼내요.
- **cron-job.org** 는 바깥에서 정해진 주기(예: 6시간)마다 우리 앱의 특정 주소를 호출해서, 잠들어 있던 앱을 깨우고 "수집 한 번 돌려" 하고 시켜요.
- 앱이 기사를 긁어오는 실제 출처는 **구글 뉴스 RSS**(뉴스), **각 기관 사이트**(게시판), **유튜브/인스타**(소셜) 예요.

왜 이렇게 나눠놨냐면 — 무료로 오래 돌리려고요. Render 무료는 앱만 띄워주고 데이터는 안 지켜주니까(재시작하면 초기화), 데이터는 Neon으로 빼서 지키고, 무료라 자꾸 잠드는 문제는 cron이 깨워주는 식으로 보완한 거예요.

---

## 2. 외부 서비스 3개 — 무엇을·왜·어디에 설정했나 (제일 중요)

### ② Render — 앱을 띄우는 곳 (호스팅)
- 주소: dashboard.render.com / 실제 서비스 URL은 `https://ncfoundation-collector.onrender.com`
- GitHub 저장소(`kksh0379/ncfoundation-collector`)와 연결돼 있어서, 코드를 GitHub에 올리면(push) Render가 그걸 받아 다시 빌드·배포해요.
- **무료 플랜의 결정적 특징 2가지**:
  1. 약 **15분간 아무도 안 들어오면 잠들어요**(spin down). 그 상태에서 처음 누가 들어오면 깨어나는 데 **30~50초** 걸려요. 그래서 "페이지가 작동하지 않습니다"가 떠도 1~2분 뒤 새로고침하면 대개 열려요.
  2. 잠들면 앱 안에 있는 자동 스케줄러도 같이 멈춰요. 그래서 확실한 정기 수집은 바깥의 cron이 깨워주는 방식(③)으로 해결했어요.
- 여기서 우리가 실제로 만진 건 **환경변수(Environment)** 예요 → 4번 참고.
- 배포가 됐는지 확인: 화면 아래 푸터의 버전이 올라갔는지 보면 돼요(예: v39). 자동배포가 안 되면 Render에서 **Manual Deploy → Deploy latest commit** 을 누르면 최신 코드로 다시 배포돼요.

### ③ Neon — 데이터를 저장하는 곳 (Postgres 데이터베이스)
- 주소: console.neon.tech
- 여기가 **핵심**이에요. 수집한 기사가 전부 여기에 저장돼서, Render가 재배포·재시작돼도 **데이터가 유지**돼요.
- 우리가 한 일: Neon에서 프로젝트(`kksh0379`)를 만들고, 거기서 준 **연결 문자열(Connection string)** 을 복사해서 Render 환경변수 `DATABASE_URL` 에 붙여넣었어요.
  - 연결 문자열은 `postgresql://사용자:비번@호스트-pooler.../neondb?sslmode=require&channel_binding=require` 이런 모양이에요.
  - 참고로 앱 코드가 연결할 때, Neon pooler와 궁합이 안 좋은 `channel_binding` 파라미터는 자동으로 떼어내고, SSL은 `sslmode=require`로 강제해요. 그래서 문자열을 그대로 붙여도 잘 돼요.
  - Neon 무료도 안 쓰면 DB(compute)가 잠들어서 첫 연결이 잠깐 느릴 수 있어요. 그래서 연결 풀을 "유휴 연결을 안 붙잡고, 쓸 때 살아있는지 확인하고, 연결 대기 여유를 15초"로 설정해뒀어요(예전에 "couldn't get a connection after 30s" 나던 걸 이걸로 잡았어요).
- **연결됐는지 확인법**: 화면 아래 푸터에 **☁ 영구저장**(초록)이 보이면 Neon 연결 성공이에요. **⚠ 임시저장**(주황)이면 아직 SQLite(임시)로 도는 거라 재시작 때 날아가요.
- **비번을 바꾼 적 있어요**: 연결 문자열이 노출돼서 Neon 콘솔에서 비번을 Reset 했고, 새 문자열로 Render의 `DATABASE_URL`을 교체했어요. 앞으로도 비번을 바꾸면 → Neon에서 새 문자열 복사 → Render `DATABASE_URL` 값만 교체 → 저장(자동 재배포) 순서면 돼요.

### ① cron-job.org — 정해진 시간마다 자동으로 수집시키는 곳
- 주소: console.cron-job.org
- 무료 Render는 잠들면 스스로 수집을 못 하니, 바깥에서 주기적으로 우리 앱을 깨우고 수집을 시키는 역할이에요.
- 우리가 한 일: cronjob 하나를 만들었어요.
  - 호출 URL: `https://ncfoundation-collector.onrender.com/api/cron?token=<CRON_TOKEN>`
  - 주기: 예를 들어 6시간마다.
  - 여기 `<CRON_TOKEN>` 은 아무나 이 주소를 못 부르게 막는 **비밀 토큰**이고, Render 환경변수 `CRON_TOKEN` 에 넣은 값과 **글자까지 똑같아야** 해요. (우리가 쓴 값은 Render 환경변수에 있어요. 이 문서엔 비밀이라 안 적었어요.)
- **작동 확인법**: 그 URL을 브라우저로 직접 열어보면 `{"ok": true, "started": [...]}` 가 나오면 정상이에요. `started`가 빈 배열(`[]`)이면 "이미 수집이 돌고 있어서 새로 안 띄웠다"는 뜻이라 이것도 정상이에요. `{"error":"unauthorized"}` 가 나오면 토큰이 안 맞는 거예요.

---

## 3. 로그인/화면 구조

- 화면은 탭 3개예요: **뉴스·기사 / 서비스 게시판 / 소셜 채널**.
- 위쪽에 **관리자 로그인** 버튼이 있어요. 비밀번호는 `ADMIN_PW` 환경변수 값(설정 안 하면 코드 기본값)이에요.
  - 로그인해야만 **상태 확인 · 수집 실행 · 수집 기간 선택 · 📓 노트(이 문서)** 가 보이고 동작해요. 일반 방문자는 **보기만** 가능해요.
  - 편의를 위해, 로그인에 성공하면 비번을 브라우저에 저장해 뒀다가 다음에 로그인 창을 열면 **자동으로 채워줘요**(자동 로그인은 아니고, 버튼만 누르면 됨). 로그아웃하면 세션은 끊기지만 저장된 비번은 자동채움용으로 남아요.
- 아래쪽 **푸터**는 항상 보이는 얇은 바예요. 저작권(© 2026 김상화)·버전·저장상태(☁/⚠)가 표시돼요.

---

## 4. 환경변수 (Render → Environment에 넣는 값들) — 가장 자주 볼 부분

Render 서비스 화면에서 상단 메뉴(모바일은 `Deploys ▾` 드롭다운) → **Environment** 로 들어가면 있어요. 각 항목의 의미:

- **`DATABASE_URL`** — Neon 연결 문자열. **이게 있어야 데이터가 영구 저장**돼요. 지우면 임시 SQLite로 돌아가 재시작 때 초기화돼요. (설정함)
- **`CRON_TOKEN`** — 외부 크론이 `/api/cron`을 부를 때 쓰는 비밀 토큰. cron-job.org URL의 `token=` 값과 똑같아야 해요. (설정함)
- **`ADMIN_PW`** — 관리자 로그인 비밀번호. 설정 안 하면 코드의 기본값을 써요. 운영에선 여기서 바꾸는 걸 권장해요.
- **`SECRET_KEY`** — 로그인 세션을 안전하게 서명하는 값. 아무 긴 무작위 문자열로 설정해두면 더 안전해요.(권장)
- **`BACKFILL_DAYS`** — 최초 자동 수집(백필) 기간. 기본 `730`(=2년). 더 깊게 긁고 싶으면 `1825`(5년) 등으로 올릴 수 있어요. (선택)
- **`BATCH_DAYS`** — 정기 배치가 훑는 기간. 기본 `30`(일). (선택)
- **`AUTO_BACKFILL`** — 앱 시작 시 DB가 비었으면 자동 백필할지. 기본 켜짐(`1`). `0`이면 끔. (선택)
- **`ENABLE_SCHEDULER`** — 앱 내부 4시간 스케줄러 켜고 끄기. 기본 켜짐(`1`). (선택)

> 🔐 중요: 비밀값(DB 연결문자열·비번·토큰)은 **코드나 GitHub, 채팅에 절대 적지 말고** 오직 Render 환경변수에만 두세요. 이 문서에도 실제 값은 안 적고 이름만 적었어요.

---

## 5. 수집이 도는 3가지 경로 (자세히)

1. **최초 자동 백필 (딱 한 번, 깊게)**
   - 앱이 처음 떠서 뉴스 DB가 **완전히 비어 있을 때만** 자동으로 실행돼요.
   - 뉴스는 **최근 2년치**(BACKFILL_DAYS)를, 게시판·소셜도 같이 긁어요. (5년은 너무 오래 걸리고 Neon이 자다 깨는 구간이 길어져서 2년으로 줄였어요.)
   - 한 번 채워지면 다음부터는 "데이터 있음 → 건너뜀". 그래서 배포할 때마다 다시 긁지 않아요.
   - 이게 있어서 **배포만 해두면 사람이 버튼을 안 눌러도 기사가 채워져** 방문자에게 바로 보여요.

2. **정기 배치 (주기적으로, 최신만)**
   - 앱 내부 스케줄러(4시간마다) + 바깥 cron-job.org 호출로 돌아가요.
   - 뉴스는 **최근 30일**(BATCH_DAYS)만 훑어서 새로 올라온 기사를 보충해요. (5년을 매번 다시 긁으면 낭비라서 역할을 나눴어요.)

3. **수동 (관리자가 직접)**
   - 로그인한 관리자가 **수집 실행** 버튼을 눌러요. 이때 **수집 기간**(1개월~5년)을 골라서 원하는 만큼 긁을 수 있어요.

### 공통 규칙
- **증분 수집**: 이미 DB에 있는 기사(URL 기준)는 다시 처리 안 해요. 그래서 두 번째 수집부터 빠르고, 중복이 안 쌓여요. (이때 비교는 URL만 가볍게 읽어요 — 예전에 본문 전체를 다 읽어서 느려지고 "수집 대기"에서 멈추던 걸 이걸로 고쳤어요.)
- **멈춤 방지**: 어떤 수집 작업이 30분 넘게 "실행 중"이면 멈춘 걸로 보고 자동으로 풀어줘서, 몇 시간씩 "수집 대기"에 갇히지 않아요.
- **진행 표시**: 수집은 서버에서 백그라운드로 돌고, 화면은 상태를 폴링해서 "RSS 수집 N/총 구간 → 새 기사 요약 N/M → 저장·그룹화 → 완료" 처럼 단계가 흘러가요. **휴대폰 화면을 꺼도 서버 수집은 계속되고**, 돌아오면 자동으로 상태에 다시 붙어요.

---

## 6. 뉴스 수집의 핵심 디테일 (여기가 제일 까다로웠어요)

- **카테고리 2개**:
  - **재단** = 키워드 `엔씨문화재단`, `NC문화재단`
  - **본사** = `엔씨소프트`, `NCSOFT`, `NC`, `엔씨` + 자회사 `엔씨에이아이(NC AI)`, `엔씨QA`, `엔씨IDS`, `퍼스트스파크 게임즈`, `빅파이어 게임즈`, `루디우스 게임즈`
  - `NC`/`엔씨` 는 영어 단어 속 'nc'(announce 등)나 야구단 **NC 다이노스** 기사를 마구 잡아와요. 그래서 영문 키워드는 "단어 단위"로만 매칭하고, `다이노스·프로야구·야구·KBO·구단` 같은 단어가 들어간 기사는 제외해요.
- **왜 5년치가 잘 안 나왔었나 (중요)**: 구글 뉴스 RSS는 **한 번 요청에 최대 ~100건, 최신 위주**로만 줘요. 그래서 `최근 5년`으로 요청해도 구글이 100건에서 잘라버려요(기사 많은 본사는 몇 달치만 나옴).
  - **해결**: 기간을 **120일 구간으로 쪼개서**(`after:날짜 before:날짜` 검색) 각 구간마다 100건씩 받아요. 그러면 과거까지 깊게 긁혀요. 5년이면 구간이 16개쯤 생기고, 키워드(13개)×구간(16개) ≈ 200번 요청을 병렬로 돌려요.
  - 그래도 구글이 아주 오래된 기사는 색인이 적어서 5년을 꽉 채우진 못할 수 있어요(특히 재단). 구간을 더 잘게 쪼개면 촘촘해지지만 느려져요.
- **본문(요약)**: 구글 뉴스 링크는 리다이렉트라서 원문 URL을 바로 못 얻어요. 그래서 구글의 내부 API(batchexecute)로 실제 기사 주소를 복원해 본문을 뽑고, 실패하면 RSS 요약(snippet)을 대신 써요. **저장 키(url)는 항상 구글 링크로 고정**하고(그래야 다음 수집 때 "이미 있음"으로 인식돼 증분이 됨), 복원한 실제 기사 주소는 `source_url`에 따로 넣어 "원문 보기" 링크에 써요.
- **동일 기사 묶음(아코디언)**: 같은 보도자료가 여러 매체에 뿌려지면, 제목/본문 유사도(글자 n-gram TF-IDF 코사인, 임계값 0.55)로 묶어서 `group_key`를 매겨요. 화면에선 대표 기사 하나만 카드로 보이고, "같은 기사 N건 매체별 보기"를 누르면 나머지가 펼쳐져요.

---

## 7. 게시판/소셜 상태

- **나의AAC**: 정상 수집(서버가 HTML을 그대로 주는 사이트라 선택자로 잘 긁힘).
- **프로젝토리**: 예전엔 SSL 인증서 체인 문제로 접속 자체가 실패했는데, "SSL 검증 실패 시 검증을 끄고 재시도"로 뚫었고, 실제 목록 선택자(`a.info__title-link` 등)를 넣었어요.
- **FAIR AI · 대표 홈페이지**: 목록을 JavaScript로 그리는 SPA라, 서버가 받는 정적 HTML엔 글 목록이 없어요. 그래서 지금은 "구현 예정". 내부 JSON API를 찾아 붙이거나 헤드리스 브라우저가 필요해요. (`/api/inspect`에 API 후보를 찾아주는 도구를 넣어놨어요.)
- **유튜브**: 채널 RSS로 최신 영상 수집 정상.
- **인스타그램**: 로그인 없는 공개 API로 시도하지만, 데이터센터 IP(무료 호스팅)는 인스타가 자주 막아서 0건일 수 있어요. 확실히 하려면 공식 Graph API 토큰이 필요해요.

---

## 8. 자주 겪는 문제 & 해결

- **"페이지가 작동하지 않습니다"** → 무료 Render가 자다 깨는 중(30~50초). 1~2분 뒤 새로고침.
- **푸터가 ⚠ 임시저장** → `DATABASE_URL`이 반영 안 됨. Render 환경변수 확인 + 재배포 기다리기.
- **수집이 "couldn't get a connection"** → Neon 연결 실패. 연결문자열/재배포 확인(코드가 channel_binding 제거·SSL 처리는 자동으로 함).
- **`/api/cron`이 unauthorized** → URL의 token과 Render `CRON_TOKEN` 값이 다름. 값 일치 + 재배포 확인.
- **수집이 "수집 대기"에서 안 움직임** → 30분 지나면 자동으로 "중단됨"으로 풀리니, 새로고침 후 다시 수집. 계속 그러면 Render의 Logs에서 `[crawl]`/`[backfill]`/`[google]` 줄을 봐야 해요.
- **기사가 과거까지 안 내려감** → 구글 RSS의 한계(오래된 색인 부족). 구간을 더 잘게(예: 60일) 하면 개선되지만 느려져요.

---

## 9. ⛔ 데이터가 날아가는 행동 (절대 하지 말 것)

1. Render에서 **`DATABASE_URL` 삭제** → 임시 SQLite로 돌아가 초기화됨.
2. **Neon 콘솔에서 DB/프로젝트 삭제·리셋**.

이 둘만 안 건드리면 데이터는 영구 보존돼요. 배포·재시작은 안전해요.

---

## 10. 링크 모음
- 사이트: https://ncfoundation-collector.onrender.com
- GitHub 저장소: https://github.com/kksh0379/ncfoundation-collector
- Render(호스팅): https://dashboard.render.com
- Neon(DB): https://console.neon.tech
- cron-job.org(정기 실행): https://console.cron-job.org

---

## 11. 코드 어디에 뭐가 있나 (수정할 때 참고)
- `app.py` — Flask 앱. 라우트(화면·API), 로그인, 수집 작업 실행/상태, 스케줄러/크론/백필.
- `collector/google_news.py` — 뉴스 수집(카테고리·키워드·구간 분할·본문 복원·필터).
- `collector/boards.py` — 게시판 수집(사이트별 선택자).
- `collector/social.py` — 유튜브/인스타 수집.
- `collector/dedup.py` — 동일 기사 묶기(group_key) 로직.
- `collector/db.py` — 저장소. `DATABASE_URL` 있으면 Postgres, 없으면 SQLite.
- `collector/fetcher.py` — HTTP 요청 헬퍼(타임아웃·SSL 폴백 등).
- `templates/index.html`, `static/js/app.js`, `static/css/style.css` — 화면.
- `DEVNOTE.md`(이 문서), `CHANGELOG.md`(일자별 패치 내역).
