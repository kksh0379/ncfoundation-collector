# 🛠 개발노트 (초보자용 / 운영 안내)

> 나중에 다시 봐도 이해할 수 있게, **무엇을·왜·어디에** 설정했는지 쉬운 말로 정리한 문서입니다.
> (변경 이력은 [`CHANGELOG.md`](CHANGELOG.md) 참고)

---

## 1. 이 프로젝트가 뭐냐
엔씨문화재단 관련 **뉴스/기사 · 서비스 게시판 · 소셜 채널**을 자동으로 모아서 모바일 웹으로 보여주는 수집기입니다.
- 화면(웹) = Flask(파이썬) 앱 하나
- 수집한 데이터 = 외부 데이터베이스(Postgres)에 영구 저장
- 정해진 주기로 자동 수집(배치)

## 2. 전체 그림 (누가 무슨 일을 하나)

```
 [사람 브라우저] ──본다──▶ 사이트(뉴스/게시판/소셜)
        ▲                         │
        │                         ▼
 [cron-job.org] ──주기 호출──▶ [Render: 우리 앱(Flask)] ──저장/조회──▶ [Neon: Postgres DB]
   (정기 실행)                  (인터넷에서 기사 수집)                 (데이터 영구 보관)
                                     │
                                     └──수집──▶ 구글 뉴스 RSS / 게시판 사이트 / 유튜브·인스타
```

- **Render**: 우리 앱을 인터넷에 띄워주는 곳(호스팅). GitHub에 코드 올리면 자동으로 배포.
- **Neon**: 데이터(기사)를 저장하는 외부 데이터베이스(Postgres). 앱이 재시작돼도 데이터가 안 날아감.
- **cron-job.org**: 정해진 시간마다 우리 앱의 주소를 대신 눌러줘서(=깨워서) 자동 수집을 돌게 하는 무료 서비스.

## 3. 외부 서비스 3종 — 무엇을, 왜, 어디에 설정했나

### ① Render (호스팅) — dashboard.render.com
- **역할**: GitHub의 코드를 받아 앱을 인터넷에 띄움. 코드가 GitHub에 push되면 자동(또는 수동)으로 재배포.
- **주소**: https://ncfoundation-collector.onrender.com
- **무료 플랜 주의**: 약 15분간 아무도 안 들어오면 **잠듦**(다음 접속 시 30~50초 깨어나는 시간). 그래서 정기 자동 수집은 cron이 깨워줌.
- **여기서 설정한 것 = 환경변수(Environment)** → 4번 표 참고.

### ② Neon (데이터베이스) — console.neon.tech
- **역할**: 수집한 기사를 저장하는 Postgres DB. **여기 있어서 재배포/재시작해도 데이터 유지**.
- **설정한 것**: Neon에서 만든 **연결 문자열(Connection string)** 을 Render 환경변수 `DATABASE_URL` 에 넣음.
  - 연결 문자열 형태: `postgresql://<사용자>:<비번>@<호스트>-pooler.../neondb?sslmode=require`
  - (앱이 알아서 `channel_binding` 파라미터는 떼고, SSL은 require로 처리함)
- **확인법**: 사이트 상단 배지가 **"☁ 영구저장"**(초록)이면 Neon 연결됨. "⚠ 임시저장"이면 연결 안 된 것.

### ③ cron-job.org (정기 실행) — console.cron-job.org
- **역할**: 정해진 주기(예: 6시간)마다 우리 앱의 수집 트리거 주소를 호출 → 앱을 깨우고 수집 배치 실행.
- **설정한 것**: 새 cronjob 하나.
  - **URL**: `https://ncfoundation-collector.onrender.com/api/cron?token=<CRON_TOKEN>`
  - **주기**: 6시간마다(원하는 대로)
  - `<CRON_TOKEN>` = Render 환경변수 `CRON_TOKEN` 에 넣은 값과 **똑같아야** 함(비밀번호 같은 역할).
- **확인법**: 그 URL을 브라우저로 열어 `{"ok": true, ...}` 나오면 정상. cron-job.org 실행이력에 200 뜨면 정기 실행 OK.

## 4. 환경변수 (Render → Environment 에 넣는 값들) ⭐가장 중요

| 이름 | 용도 | 설정 여부 / 메모 |
|---|---|---|
| `DATABASE_URL` | Neon Postgres 연결(데이터 영구 저장) | **설정함**. 없으면 임시 SQLite로 동작(재시작 시 초기화) |
| `CRON_TOKEN` | 외부 크론이 `/api/cron` 부를 때 쓰는 비밀 토큰 | **설정함**. cron-job.org URL의 token과 동일해야 함 |
| `ADMIN_PW` | 관리자 로그인 비밀번호 | 미설정 시 코드 기본값 사용. **운영 시 여기서 새 비번으로 바꾸는 걸 권장** |
| `SECRET_KEY` | 로그인 세션 서명용(보안) | 권장: 아무 긴 무작위 문자열로 설정 |
| `BACKFILL_DAYS` | 최초 자동 백필 기간(기본 1825=5년) | 선택 |
| `BATCH_DAYS` | 정기 배치 수집 창(기본 30일) | 선택 |
| `AUTO_BACKFILL` | 시작 시 빈 DB 자동 백필 on/off(기본 1) | `0`이면 끔 |
| `ENABLE_SCHEDULER` | 내부 4시간 스케줄러 on/off(기본 1) | `0`이면 끔 |

> 🔐 **비밀값(비번·토큰·DB 연결문자열)은 코드/깃/채팅에 적지 말고 오직 Render 환경변수에만** 두세요.
> 이 문서에도 실제 값은 적지 않았습니다(이름만).

## 5. 수집이 도는 3가지 방식

1. **최초 자동 백필** — 앱이 처음 떠서 DB가 비어 있으면 **딱 1번**, 뉴스 **5년치**(+게시판·소셜)를 자동 수집. 데이터가 차면 다음부터는 건너뜀.
2. **정기 배치** — 4시간마다(내부 스케줄러) + cron-job.org 호출. 뉴스는 **최근 30일**만 훑어 새 기사 보충.
3. **수동** — 관리자 로그인 후 "수집 실행" 버튼(기간 선택 가능: 1개월~5년).

- **증분 수집**: 이미 저장된 기사(URL 기준)는 다시 처리 안 함 → 두 번째부터 빠름, 중복 없음.
- **카테고리**: 재단(엔씨문화재단) / 본사(엔씨소프트·자회사). 야구(NC 다이노스)는 제외.
- **동일 기사 묶음**: 같은 보도자료가 여러 매체에 뿌려지면 유사도로 묶어 "아코디언"으로 표시.

## 6. 관리자 로그인
- 헤더의 **"관리자 로그인"** → 비밀번호 입력(= `ADMIN_PW`).
- 로그인해야 **상태 확인 · 수집 실행 · 개발노트** 버튼이 보이고 동작함. 일반 방문자는 **조회만**.

## 7. 자주 겪는 문제 & 해결
- **"페이지가 작동하지 않습니다"**: 무료 Render가 자다 깨는 중(30~50초). 1~2분 뒤 새로고침.
- **배지가 "⚠ 임시저장"**: `DATABASE_URL` 이 반영 안 됨 → Render 환경변수 확인 + 재배포 대기.
- **수집이 "couldn't get a connection ..."**: DB 연결 실패 → Neon 연결문자열/재배포 확인(앱이 자동으로 channel_binding 제거·SSL 처리함).
- **`/api/cron` 에서 `{"error":"unauthorized"}`**: URL의 token ≠ Render의 `CRON_TOKEN`. 값 일치 + 재배포 확인.
- **수집이 오래 걸림**: 첫 백필(5년)만 오래 걸림. 이후 증분이라 빠름. 진행률은 화면에 `N/총`으로 표시.
- **화면 껐다 켜도 수집 유지**: 수집은 서버에서 도니까 브라우저 꺼도 계속됨. 돌아오면 자동으로 상태 다시 붙음.

## 8. ⛔ 데이터가 날아가는 행동 (하지 말 것)
1. Render에서 **`DATABASE_URL` 삭제** → 임시 SQLite로 돌아가 초기화됨
2. **Neon 콘솔에서 DB/프로젝트 삭제·리셋**
→ 이 둘만 안 건드리면 데이터는 영구 보존.

## 9. 링크 모음
- 사이트: https://ncfoundation-collector.onrender.com
- GitHub: https://github.com/kksh0379/ncfoundation-collector
- Render: https://dashboard.render.com
- Neon: https://console.neon.tech
- cron-job.org: https://console.cron-job.org

## 10. 초보 용어 사전
- **배포(Deploy)**: 코드를 실제 인터넷 서버에 올려 동작시키는 것.
- **환경변수(Environment Variable)**: 코드 밖에서 넣는 설정값(비번·연결정보 등). 코드에 안 박고 여기 둠.
- **DB(데이터베이스)**: 데이터를 저장하는 창고. 여기선 Neon Postgres.
- **크론(cron)**: "정해진 시간마다 실행"을 뜻하는 예약 실행. cron-job.org가 대신 해줌.
- **배치(batch)**: 모아서 한 번에 처리하는 작업. 여기선 "정기 수집".
- **증분(incremental)**: 전체가 아니라 "새로 생긴 것만" 처리.
- **SSL**: 통신 암호화. DB 연결에 필요(`sslmode=require`).
- **콜드 스타트**: 자던 서버가 깨어나는 데 걸리는 첫 지연.
